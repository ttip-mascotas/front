import "package:flutter/material.dart";

class PetsDivider extends StatelessWidget {
  const PetsDivider({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.purple.shade200,
    );
  }
}
